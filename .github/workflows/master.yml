name: 'master'

on:
  push:
    branches:
      - 'master'
    tags:
      - 'v*.*.*'

jobs:
  build_lib:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout source
        uses: actions/checkout@v2
      - name: Setup node
        uses: actions/setup-node@v1
        with:
          node-version: 16
      - name: Install packages
        run: npm ci --legacy-peer-deps --ignore-scripts
      - name: Unit test
        run: |
          npm run test:ci
      - name: Build lib & app
        run: |
          npm run build
          npm run build:lib
      - if: startsWith(github.ref, 'refs/tags/v')
        name: Upload lib
        uses: actions/upload-artifact@v2
        with:
          name: ng-qrcode-svg
          path: dist/ng-qrcode-svg

  publish_npm:
    needs:
      - build_lib
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - name: Checkout source
        uses: actions/checkout@v2
      - name: Setup node
        uses: actions/setup-node@v1
        with:
          node-version: 16
          registry-url: https://registry.npmjs.org/
      - name: Download lib
        uses: actions/download-artifact@v2
        with:
          name: ng-qrcode-svg
          path: /tmp/ng-qrcode-svg
      - name: Publish lib
        run: |
          cd /tmp/ng-qrcode-svg
          ls -ltra .
          npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NODE_AUTH_TOKEN }}
